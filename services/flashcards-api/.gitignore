# compiled output
/dist
/node_modules

# logs
*.log
npm-debug.log*

# env — every variant (.env, .env.local, .env.prod, ...) except the
# checked-in template
.env*
!.env.example

# IDE
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea

# coverage
/coverage

# OS
.DS_Store
